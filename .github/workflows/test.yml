name: Test ASDF
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@main

      - name: 'ASDF Install'
        uses: asdf-vm/actions/install@v1

      - name: 'Test Tools'
        run: |
          terraform --version
          node --version
          npm --version
